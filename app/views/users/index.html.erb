<h1>Listing users</h1>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @users.each do |user| %>
  <tr>
    <td><%= link_to 'Show', user.becomes(User) %></td>
    <td><%= link_to 'Edit', edit_user_path(user.becomes(User)) %></td>
    <td><%= link_to 'Destroy', user.becomes(User), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New User', new_user_path %>
