require 'test_helper'

class UserTypeTest < ActiveSupport::TestCase
  # Replace this with your real tests.
  test "administratorExists" do
    #puts "users; " + YAML::dump(UserType.first)
    assert true
    #assert_equal "admin", users[0].name, "admin not found"

  end
end
